{
  "name": "@moodle-dl-ext/background",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "pre-build": "rm -rf build dist && mkdir -p build dist",
    "build": "webpack --mode=production --node-env=production",
    "package": "mkdir -p ../../output/background && cp -r dist/main.js ../../output/background",
    "build:dev": "webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production",
    "watch": "webpack --watch"
  },
  "author": "rssws",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/chrome": "^0.0.194",
    "ts-loader": "^9.3.1",
    "typescript": "^4.7.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
